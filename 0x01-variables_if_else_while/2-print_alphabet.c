#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char zeus = 'a';

	for (zeus = 'a'; zeus <= 'z'; zeus++)
		putchar(zeus);

	putchar('\n');

	return (0);
}
