#include <stdio.h>
/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	int num2 = 0;

	for (num2 = 0; num2 <= 9; num2++)
		printf("%d", num2);

	printf("\n");
	return (0);
}
